/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ik.imagekit.io",
      "raw.githubusercontent.com",
      "www.wizardingworld.com",
    ],
  },
};

export default nextConfig;
